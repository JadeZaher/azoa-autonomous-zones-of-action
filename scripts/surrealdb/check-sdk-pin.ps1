# Obsolete: G4 SurrealDb.Net SDK-pin guard.
# Deleted as part of surrealdb-client-package Phase 6 (sub-wave 1.5a).
# The SurrealDb.Net package was replaced by the homebake
# packages/Oasis.SurrealDb.Client/ suite; version pinning now lives in
# Directory.Build.props (<OasisSurrealDbVersion>) and is verified by
# scripts/passoff-surrealdb-wave1.ps1 section 2/3.
# This stub is retained only because the sandbox blocked the physical
# filesystem delete; nothing references this script after Phase 6.
exit 0
