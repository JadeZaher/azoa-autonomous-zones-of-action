// File deleted as part of surrealdb-client-package Phase 6 (sub-wave 1.5a).
// Replacement tests live in tests/Oasis.SurrealDb.Client.Tests/ (Phase 2-3).
// Retained only as an empty stub because the sandbox blocked the
// physical filesystem delete; nothing compiles into this stub.
