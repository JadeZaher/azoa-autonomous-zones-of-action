// File deleted as part of surrealdb-client-package Phase 6 (sub-wave 1.5a).
// Replacement lives in packages/Oasis.SurrealDb.Client/Query/.
// Directory.Build.targets excludes Core/SurrealDb/** from the WebAPI compile
// glob; this file is retained only as a stub because the sandbox blocked
// the physical filesystem delete on the worker that performed the rewrite.
